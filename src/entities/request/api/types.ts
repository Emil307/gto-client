export interface RequestDTO {
  surname: string;
  name: string;
  patronymic: string;
  birthDate: string | null;
  email: string;
  region: string;
  category_id: string;
  phone: string;
  result_minutes: string;
  result_seconds: string;
}
